# ============================================================
# アプリ設定ファイル
# ============================================================

design:
  theme: "windows-xp"

features:
  trains: true
  buses: true
  delays: true

trains:
  station_id: 18
  min_minutes_until: 15
  max_trains_per_direction: 6

buses:
  stop_name: "同志社香里"
  stop_numbers:
    - 1
    - 2

delays:
  source: "yahoo"
  use_ai: true

intervals:
  train_seconds: 60
  bus_seconds: 60
  delay_seconds: 600
